## Last Session (2026-08-19, adaptive re-planning shelved; token-count display; real plan_execute evidence)
**Status:** Ready for Next Phase
**Working on:** Picked up "Adaptive re-planning" (Priority Order #1 at the time). Full plan-mode cycle: an Explore agent grounded the design in `plan_execute.py`'s actual code (confirmed the real structural gap — no retry/success concept exists today, once `react_run` returns for a step the loop just moves on regardless of outcome), then a Plan agent produced a complete, detailed implementation design (retry wrapper mirroring `ralph.py::_run_attempt`, a 3-outcome LLM-judged escalation classifier, a circuit breaker reusing the plan-approval gate for hand-back). Mid-plan-mode, the user asked directly "are we reaching too far" — checked rather than reassured, and confirmed via direct grep: **zero example agents in this project use `loop: plan_execute`**. Building substantial adaptation logic for a code path with no real users would repeat the exact "no code-level tweaks without hard evidence" mistake this project already caught and reversed once (the prompt-override-file retraction during the thrashing-breaker build), just at much larger scale (six new functions, a full execution-loop restructure). Shelved the feature — design preserved in `docs/roadmap.md`'s Ideas section, not lost — in favor of the user's own chosen path: build a real `plan_execute` agent first and see if the limitation actually bites.
**Next step:** Done. Built `agents/competitor-research` (`loop: plan_execute`, same local LM Studio backend as `agents/researcher`, instructions tuned so plan generation produces one step per competitor/product) and ran it live: "compare Slack, Microsoft Teams, and Discord for a small business team." Real result, not hypothetical: plan generation produced exactly the intended one-step-per-competitor shape, critique approved immediately, all 3 steps executed with zero thrashing and zero tool errors, and the final summary was accurate and properly sourced for all three companies. One genuinely interesting real observation logged in the roadmap rather than acted on: mid-step 1, the model hallucinated "I've completed the research on all three" despite having only searched Slack — a premature self-reported completion — but it didn't derail anything, because `plan_execute.py`'s Phase 2 loop never reads or trusts a step's self-report at all, it just executes the fixed step list regardless. The rigidity adaptive re-planning would have relaxed turned out to be a *strength* in this run, not a limitation — the same "don't trust self-report, use structure" principle already behind `completion_check`/the thrash guard, this time arrived at organically rather than by design. First real evidence point, and it argues against building the shelved feature, not for it. Also shipped, smaller and separate: the per-turn console line now shows real `response.usage` token counts in k (`Turn 3/25 | $0.0000/$1.00 | 2.1k in / 0.3k out`) — a genuine quick win requested directly ("even in k tokens would be informative") after `%`-of-context-window was judged not worth the LM-Studio-query complexity right now. `pytest tests/unit -q` → 678 passed, ruff/mypy/radon all clean (doc/config-only changes this session beyond the token-count line, which had its own TDD cycle). Also captured, not built: a `docs/roadmap.md` Ideas entry mapping the real complexity of cloud-direct/OpenRouter/HF-hosted/local provider tiers, with verified (not assumed) research on Ollama/llama.cpp/LM-Studio capability differences and Apple Silicon MLX-vs-GGUF speed tradeoffs — explicitly flagged by the user as needing real thought before it becomes buildable, not a task to start now.
**Notes:** Two live runs this session hit the exact same unhandled `EOFError` crash already logged against the "Permission/approval UX for no-terminal contexts" roadmap item (interactive `_domain_prompt`/`_plan_prompt` calling `input()` with non-interactive stdin) — worked around each time (pre-approving domains via `.allowed_domains.yaml`, piping `y` for plan approval) rather than fixed, consistent with that item's own "log it for later" instruction from the prior session. Confirms the bug is real and will be hit again by anything scripted/non-interactive, not a one-off.

## Last Session (2026-08-16, OpenAI Chat Completions streaming + researcher agent)
**Status:** Ready for Next Phase
**Working on:** Built a `researcher` example agent (news roundup / competitive research, `web_search`+`web_fetch`+`write_file`, react loop) pointed at a local LM Studio model (`qwen/qwen3-4b-thinking-2507`) to dogfood the last two sessions' work (completion_check, thrash guard) against a real weak local model. First real run exposed two genuine things, not hypothetical: (1) the model burned its entire turn (1207 output tokens) reasoning in circles about how to format a tool call and never actually emitted one — diagnosed from the raw API response (`finish_reason: "stop"`, not `"length"` — ruled out a truncation/max_tokens theory in favor of the real cause, the model genuinely giving up mid-thought) rather than guessed; fixed by simplifying `instructions.md` substantially and adding an explicit "call the tool immediately, don't narrate your plan first" directive. (2) The user asked to try `--stream` against the same local model and hit `runtime.py`'s existing rejection for Chat Completions/`base_url` backends — investigated before building anything: confirmed this was a real, deliberate-but-incomplete harness gap (Responses-API streaming was built, Chat-Completions streaming never was), not a technical limitation of LM Studio/OpenAI-compatible servers, and confirmed no dedicated provider was needed — both endpoints already route through one `chat()` function in `openai_provider.py`, purely on whether `base_url` is set.
**Next step:** Done and verified, TDD throughout (red confirmed before each step). Grounded in the real `openai` Python SDK's type definitions before writing anything (not guessed): `ChoiceDeltaToolCall`/`ChoiceDeltaToolCallFunction` confirm tool-call arguments stream as JSON-string fragments keyed by `index` (not `id` — only the first fragment for a given index carries `id`/`name`), and `usage` only appears on a final, empty-`choices` chunk when `stream_options: {"include_usage": true}` is requested. Built: `_merge_tool_call_delta` + `_stream_chat_completions_deltas` (new accumulator functions in `openai_provider.py`, split in two to keep radon complexity low — `stop_reason` logic deliberately copied verbatim from the non-streaming `_chat_completions_to_response`, ignoring the API's own `finish_reason`, so streaming and non-streaming behave identically); `_build_create_kwargs` (`stream`/`stream_options` now injected for the Chat Completions branch, unconditionally when streaming — matches this codebase's established "let the API reject unsupported params, don't pre-validate" policy from the multimodal work); `chat()`'s `_call()` closure restructured to route Chat-Completions-streaming through the new accumulator (confirmed via direct SDK-type reading that `client.chat.completions.create(stream=True)` returns a plain iterable, **not** a context manager, unlike `client.responses.stream()` — deliberately no `with` block); removed the now-obsolete rejection guards in both `openai_provider.py` and `runtime.py::validate_config` (ruff caught a real follow-on cleanup — the `runtime.py` guard's outer `if` became single-condition after removal and needed collapsing, not left as a redundant nested `if`). `pytest tests/unit -q` → 671 passed (up from 668); ruff clean; `mypy --strict` clean; `radon cc --min D` → none (`_stream_chat_completions_deltas` C(13), `_merge_tool_call_delta` A(5)). `docs/features.md` streaming bullet updated; `docs/roadmap.md` gained one Ideas entry (the file-split-deferred debt — user explicitly asked to defer, not skip tracking) and the `researcher` agent (untracked in docs, it's an example agent not a doc-worthy feature, matching how other example agents aren't individually documented beyond the README table).
**Notes:** Explicitly out of scope, called out rather than silently accepted: `retry.py::with_retry` has no streaming awareness (a transient error mid-stream causes a full retry-from-scratch, so `on_delta` can fire twice for one logical call) — this is pre-existing behavior already true of the Responses-streaming path, the new Chat-Completions path just inherits the same characteristic, not a new or worse one; not fixed here. **Not live-verified against the user's actual LM Studio instance** — it was unreachable from this machine throughout the session (`curl` to `http://192.168.0.13:1234/v1/models` timed out every time it was checked) — shipped on the mocked unit tests (including the critical multi-chunk tool-call-argument-concatenation test, which proves the accumulation logic isn't accidentally correct from a single-chunk case) only. Flagged clearly to the user: needs a real smoke-test once LM Studio is reachable before being trusted end-to-end.

## Last Session (2026-08-15, step-level tool-call thrashing breaker)
**Status:** Ready for Next Phase
**Working on:** Continuation of the same day's roadmap work — a long design discussion (using a prior XAML workflow-builder MCP project as a concrete real-world reference case) worked out the difference between tool-use-level self-correction (normal, needs no new mechanism) and genuine plan-level replanning, landing on a two-tier model: a cheap, mechanical step-level "thrashing breaker" as the trigger, escalating only rarely to a (separate, later-item) plan-level check. Also settled, and written into the roadmap rather than just discussed: try `instructions.md` first since a lot of this needs no harness change at all, mechanism is a backstop; every threshold becomes a `config.yaml` field, no speculative prompt-tuning infrastructure until real evidence demands it (a suggestion for one was raised and explicitly retracted the same session). Then built the thrashing breaker itself (Priority Order #1 at the time) — PRD context already mostly settled from the design discussion, so planning moved straight to grounding in real code: an Explore agent confirmed `react.py`'s exact turn-loop shape and that it has zero `Tracer` dependency (all tracing lives in `runtime.py`, via callbacks), a Plan agent then resolved the open design points (on-by-default vs. opt-in threshold, one shared threshold vs. two, repeat-firing behavior, how to integration-test something a live model can't be reliably provoked into doing).
**Next step:** Done and verified, TDD throughout (red confirmed before each step). Built: `loops/common.py::check_tool_thrashing` (new pure helper — two independent signals, consecutive same-tool error streak and identical-arguments repeat count, sharing one `thrash_threshold` config field, default 3, on by default unlike `completion_check` since a repeated failure is never legitimately wanted); `types.py`/`config.py` (`AgentConfig.thrash_threshold`, `LoopCallbacks.on_thrash_detected`); `react.py` (per-call detection inside the existing tool-call loop, but — a correctness fix caught during plan review, not by the initial sketch — the actual nudge message append happens once, *after* the whole turn's tool calls are resolved, not inside the per-call loop, because injecting a `user`-role message between two sibling `tool_result`s risked breaking Anthropic's tool_use/tool_result pairing requirement for multi-tool-call turns; locked in with a dedicated positioning test); `runtime.py`/`display.py` (`on_thrash_detected` closure + `show_thrash_warning`, exact mirror of the `on_completion_status`/`show_completion_status` pattern built earlier this session). Because `reflection`/`eval_optimize`/`plan_execute`/`ralph` all delegate to a react sub-loop, this benefits all of them with zero changes to those files; `rewoo.py`'s independent tool-execution path doesn't get it — acknowledged gap, not built. `pytest tests/unit -q` → 668 passed (up from 644); ruff clean; `mypy --strict` clean (92 files); `radon cc --min D` → none (`react.py::run` moved C(15)→C(18), still comfortably inside the C band, checked explicitly rather than assumed safe). Real, zero-mock integration test seeds a conversation history with pre-built repeated-failure turns plus an already-injected nudge and confirms it survives a live Anthropic API round-trip — documented explicitly as testing plumbing, not live-model misbehavior (a capable model won't reliably thrash on cue, so detection logic is covered by the deterministic mocked unit tests instead, not by trying to provoke a real model). All 12 pre-existing real-API loop-pattern integration tests re-run and passed unchanged, confirming the new on-by-default threshold doesn't interfere with any normal run. `docs/roadmap.md` (item moved to Done, Priority Order renumbered — Adaptive re-planning is now #1, no longer blocked on a pending prerequisite), `docs/features.md` updated.
**Notes:** The provider-message-pairing risk (nudge landing between two sibling `tool_result`s) was caught during the plan review step, before any code was written, by reasoning about the Anthropic message-translation layer's known constraints rather than discovering it via a failing live-API call later — same "verify against real code/API behavior, don't guess" discipline this project has leaned on all session (e.g. the multimodal work's earlier `hooks.py` path-traversal-reuse catch). Two smaller, real corrections during test-writing: an initial `test_success_resets_error_streak` reused the same tool-call arguments across calls, which would have also tripped the (separate) duplicate-args signal and made the test's own premise false — fixed by varying arguments per call to isolate the signal actually being tested. mypy --strict caught three real narrowing gaps in the new test file (`ToolResult | None` accessed without a None-check across two separate assert lines) — fixed by binding to a local variable before asserting, not by loosening the type.

## Last Session (2026-08-15, roadmap restructuring + verified task completion)
**Status:** Ready for Next Phase
**Working on:** Two threads. (1) High-level strategic discussion on capabilities/gaps, converging on a restructured `docs/roadmap.md`: dropped the moot o4-mini item, added a `Priority Order` index (explicitly exempt from the file's append-only convention), and promoted/added items (verified task completion, adaptive re-planning, concurrency-safety for per-agent-dir state, permission UX for no-terminal contexts) after a debate that corrected two scoping errors along the way — parallel tool-calls/fan-out don't belong in the "interface expansion" track (single-task execution speed, not access), and a real async-core rewrite isn't justified without a genuine high-fan-out driver (thread/process-per-request at the API boundary is sufficient). (2) Built "Verified task completion" (Priority Order #1 at the time), full PRD-then-plan-then-build cycle: PRD/plan discussion converged on a single `completion_check` config field dispatched uniformly through the normal tool-call path (named tool or `run_command` fallback), then plan mode (an Explore agent grounded the design in real code, a Plan agent produced the detailed file-by-file implementation plan) before any code was written.
**Next step:** Done and verified, TDD throughout every file (red confirmed before each implementation step). Built: `budget.py::is_exceeded()` (read-only, refactored `record()`'s limit check into a shared `_over_limits()` predicate); `tools.py::run_command` now unconditionally appends `[exit code N]` to its output (previously never read `result.returncode` at all — a failing shell command was indistinguishable from a passing one); `types.py` (`AgentConfig.completion_check`, `LoopCallbacks.on_completion_status`/`.is_budget_exceeded`); `loops/common.py::run_completion_check`/`report_completion_status` (new shared helpers — dispatch via `cb.on_tool_call`, so permissions/hooks/tracing apply exactly like any normal tool call); `reflection.py`/`eval_optimize.py` (`for...else` gates the DONE/SCORE-pass branch on the check, falls straight through unchanged when unset); `ralph.py` (bigger change — new `_run_attempt` helper feeds a failed check's output into the *same* attempt rather than a fresh restart, since discarding real failure feedback every time would defeat the point of checking; also fixed an independent pre-existing bug found during investigation, not previously known: ralph's outer retry loop never checked whether the shared `Budget` was already exceeded before starting another attempt, burning wasted attempts and re-printing the budget message once per attempt); `runtime.py`/`display.py` (`show_completion_status`, mirrors `show_budget`'s shape — closes a real gap where `cli.py` discarded a loop's return value entirely, so nothing distinguished "stopped verified" from "stopped unverified" in what the user saw); a `logger.warning` in `validate_config` when `completion_check` is set on a loop that doesn't honor it. `pytest tests/unit -q` → 644 passed (up from 588); ruff clean; `mypy --strict` clean (91 files); `radon cc --min D` → none. Real, zero-mock integration test (`tests/integration/test_real_loops.py::TestCompletionCheck`, run live against the Anthropic API, not just claimed): a deterministic counter-based checker script fails its first invocation and passes every one after — proves a genuine fail-then-retry-then-pass cycle, independent of model behavior. `docs/roadmap.md` (item moved to Done, Priority Order renumbered — Adaptive re-planning is now #1), `docs/features.md`, `README.md` updated.
**Notes:** Investigation before planning (not guessed) surfaced both the `run_command` exit-code gap and the ralph budget bug — neither was on the roadmap before this session; both were folded into scope rather than deferred, since the feature's own correctness depended on the first and the user's explicit "cognisant of budget cap" requirement depended on the second. One open design question resolved during planning rather than left for a future session: whether `run_command`'s exit-code trailer should be unconditional or nonzero-only — nonzero-only would have left a genuinely-passing shell command with no signal at all under the PASS/FAIL/exit-code convention, so unconditional was the correct (and simpler) choice.

## Last Session (2026-08-15, multimodal file handling)
**Status:** Ready for Next Phase
**Working on:** Two threads. (1) A real bug in MCP client support's collision policy, found through live use of `agents/hello` rather than a code review — checked the entire tool registry instead of what an agent actually exposes via `config.tools`, so `write_file`/`edit_file`/`list_directory` were silently unreachable (neither built-in nor MCP). Fixed same-day: check `config.tools` + names already claimed earlier in the same MCP merge, not the full registry — commit `6690e71`. (2) New capability, "Multimodal file handling" — full PRD-then-spec-then-build cycle per the project's mandated workflow, since the user wanted to skip Parallel sub-agent fan-out (no concrete driving use case yet) and treat async/concurrency as its own future phase, which moved this up instead. Extensive PRD discussion first (`docs/multimodal-plan.md`): scope split (images/PDFs = new plumbing, text/tool-supported-binary already solved via existing `tools/profile_data.py` pattern, generation explicitly out of scope), policy decisions (no static provider-capability table — same lesson as the `COST_TABLE`/o4-mini drift bug fixed earlier this project — token/history pruning policy, tmp-dir storage lifecycle, guardrails). Then a full technical spec via plan mode, validated by a dedicated Plan agent that re-read every touched file end to end before any code was written.
**Next step:** Done and verified, TDD throughout every file. Built: `agent_harness/attachments.py` (new — `Attachment` dataclass wiring, `build_attachment_from_file`, `wrap_binary_output`/`extract_and_save_binary_output` for agent-produced binary output, `prune_attachments` mirroring `loops/react.py::_with_budget_note`'s disposable-overlay pattern exactly); `tools.py` (`view_image`/`view_document` built-ins, matched by function *identity* not name in `execute_tool` to avoid MCP name-shadowing); attachment content-block branches on all three message-translation functions (`_to_anthropic_messages`, `_to_openai_messages`, `_to_openai_input`) as **separate synthetic messages** following the tool result, not fused into it — necessary since both OpenAI translation paths are string-only for tool output, verified rather than assumed; `runtime.py` (per-run `agent_dir/tmp/` lifecycle, mirrors `eval/runner.py::_clear_memory`'s existing pattern); pruning wired into both `react.py` and the independent-tool-execution-path `rewoo.py` loop (the validator caught that the latter doesn't delegate to `react_run` and would have silently skipped pruning). `pytest tests/unit -q` → 588 passed; ruff clean; mypy --strict clean (102 files); radon shows no D-or-worse anywhere in the codebase. Real, zero-mock verification: a real striped PNG (stdlib-only encoder, no new dependency) viewed via live Anthropic API, model's exact color-and-order answer proving genuine visual perception; two-image pruning confirmed live; mechanism-B binary-output pipeline confirmed via `execute_code` generating real bytes in-process with zero base64 ever reaching captured output; graceful degradation confirmed against a real local LM Studio server (both the deterministic PDF-over-Chat-Completions note and a genuine live provider rejection surfacing as `RuntimeError`, not a crash). `docs/roadmap.md`/`docs/multimodal-plan.md` updated: item moved to Done, build-order note updated (multimodal shipped ahead of its original slot, parallel fan-out deferred to a future "async phase" bucket rather than scheduled individually).
**Notes:** The Plan agent's validation pass caught **two real bugs before they shipped**, not after: (1) the obvious guardrail reuse — `hooks.py`'s path-traversal helper exempts absolute paths by design for its actual job (explicit tool arguments), but reused verbatim for a filename pulled from *untrusted tool output* it would have been a real vulnerability, since `Path("/agent/tmp") / "/etc/passwd"` silently evaluates to `/etc/passwd` in Python — fixed with a separate, stricter basename-only check; (2) the "let the API reject unsupported content, don't pre-validate" policy would have crashed the whole process — traced the real path and found zero try/except anywhere between a provider's `BadRequestError` and an unhandled traceback in `cli.py`, a general pre-existing gap this feature was just the first to make routine — fixed in `providers/retry.py`/`cli.py`. Also hit a real test-design pitfall while building live verification: an initial 40×30px test image got misread by Claude even though correctly attached (confirmed via an isolated raw-API call with zero harness code involved) — too small for reliable vision recognition, not a harness bug; fixed by using a larger test image. Separately, asking the model to faithfully reproduce a ~1500-char base64 literal inside a tool call proved fragile and unrelated to what the feature needed to prove — redesigned that test to pre-write the file directly, matching the safer pattern the other scenarios already used.

---

## Last Session (2026-08-13, MCP client support)
**Status:** Ready for Next Phase
**Working on:** Roadmap build order for "Plausible Future Capabilities" (decided this session): 1) step-level evidence for evals, 2) MCP support, 3) parallel sub-agent fan-out, 4) image/multimodal. Started #1, planned it via plan mode, then caught mid-build that the user's actual instruction was to delay the *entire* item (not just its grading half) until a real multi-step eval case exists — abandoned that plan unbuilt, moved to #2. Full plan for MCP at `~/.claude/plans/iterative-forging-marshmallow.md`, revised twice during planning (package choice switched from the official `mcp` SDK to FastMCP after the user raised they'll build an MCP server eventually; three follow-up scope questions — parallel tool calls, HTTP/SSE+auth, error-handling depth — resolved via AskUserQuestion, all recommended/narrow options chosen).
**Next step:** Done and verified, TDD throughout (red before implementation at every step). Built: `agent_harness/mcp_client.py` (`McpManager`/`McpServerSpec` — background-thread event-loop bridge to FastMCP's asyncio-only client, fail-fast-per-dead-server via catching `fastmcp.exceptions.ToolError` specifically as recoverable vs. any other exception as a transport failure); `AgentConfig.mcp_servers` + `config.py` loading; `runtime.py::prepare_runtime` wiring (tool-registry merge, name-collision skip+log, `PreparedRuntime.mcp_manager`, `finalize()` cleanup). `pytest tests/unit -q` → 529 passed; `pytest tests/integration -q` → 73/74 passed (1 pre-existing unrelated OpenAI-temperature failure, not touched this session); ruff clean; mypy --strict clean (98 files); radon shows only the pre-existing `validate_config` C-rating, no new D-or-worse. Real integration test (`tests/integration/test_mcp_real.py`, zero mocks, skips if `npx` absent) spins up the actual reference `@modelcontextprotocol/server-filesystem`. Manual real run against the live Anthropic API (throwaway agent in scratchpad, not the repo): model correctly discovered and called the MCP-provided `read_text_file` tool, got real file content back, answered correctly; built-in-name-collision skip fired correctly in the same run for `read_file`/`write_file`/`edit_file`/`list_directory`. Confirmed zero orphaned `npx`/node processes after every run (manual and automated). `docs/roadmap.md` updated: MCP moved to Done (retitled "MCP client support"), step-level-evidence marked delayed, build-order note updated, new "Scoped out of MCP client support — backlog" section added (MCP server support, HTTP/SSE+auth, parallel tool-call execution, run-abort-on-fatal-error — each with why it was cut).
**Notes:** `fastmcp-slim[client]` turned out heavier than its docs implied — the `[client]` extra transitively pulls in the full `mcp` SDK plus `starlette`/`uvicorn`/`authlib`/`keyring`/`cryptography` (auth/HTTP machinery for the remote-transport path this build doesn't use), not the lean footprint the plan assumed. Didn't change the package decision — that was about zero-migration-cost to a future MCP *server* build, not dependency weight — but worth knowing before adding more `fastmcp` surface later. Sibling roadmap item "CLI REPL: single-keypress prompt responses, no Enter required" and its still-uncaptured "minor tweak" note from last session remain open, untouched this session.

---

## Last Session (2026-08-13, bright single-letter prompt options)
**Status:** Ready for Next Phase
**Working on:** Roadmap item "CLI REPL: bright single-letter prompt options" (added 2026-08-10). TDD: added `TestPromptChoiceHighlighting` to `test_cli.py` first (red), then built `_highlight_choices()` in `cli.py` — regex-based, styles each `[x]`-bracketed choice `bold cyan`, leaves brackets/surrounding text unstyled. Wired into `_permission_prompt`, `_domain_prompt`, `_plan_prompt` (the three prompts using this bracket pattern).
**Next step:** Done and verified. `pytest tests/unit -q` → 510 passed; ruff clean; mypy --strict clean (83 files); radon shows no new D-or-worse on `cli.py`. Manually confirmed real ANSI output via `Console(force_terminal=True)` — `1;36` (bold cyan) codes land exactly on `o`/`s`/`p`/`d` and `y/n`, nothing else. `docs/roadmap.md` updated: item moved to Done.
**Notes:** Sibling roadmap item "CLI REPL: single-keypress prompt responses, no Enter required" is still open — user flagged there's a minor tweak worth bundling into that item when it's built, detail not yet captured (garbled message, asked for clarification). Check back before starting that item.

---

## Last Session (2026-08-10, plan critique + human-in-the-loop refinement)
**Status:** Ready for Next Phase
**Working on:** Resumed a session that stopped mid-verification. Code and tests for the "Plan critique + human-in-the-loop refinement" roadmap item (plan at `~/.claude/plans/abstract-doodling-wren.md`) were already fully written against the plan's design — `types.py` (`OnPlanApproval`, `LoopCallbacks.on_plan_approval`), `loops/plan_execute.py` (bounded critique/refine round, approval gate), `runtime.py` (`plan_prompt_fn` threading, `_on_plan_approval` wrapper with tracing), `cli.py` (`_plan_prompt` using the `Text`-wrapping markup-safety pattern) — plus the full test matrix the plan called for (`test_plan_execute.py::TestPlanCritique`/`TestPlanApprovalGate`, `test_runtime.py` plan-approval wiring tests, `test_cli.py::_plan_prompt` markup tests). What hadn't run yet was verification.
**Next step:** Done and verified. Found and fixed the actual reason things had stopped: `mypy --strict` failed with 4 errors in `test_plan_execute.py` — `Message.content` is `str | None`, and four assertions did a bare `"x" in m.content` / `in step_messages[0]` without narrowing. Fixed by building `step_messages` from `m.content or ""` instead of `m.content`. Re-ran full verification: `pytest tests/unit -q` → 507 passed; ruff clean; mypy --strict clean (83 files); radon shows no new D-or-worse on the three touched files. Manual real-run verification (plan's step 3): `agents/hello --loop plan_execute` against the live Anthropic API — critique round fired and approved the plan without revision, approval prompt correctly rendered step text containing brackets/backticks via `Text` (no markup corruption), rejecting with `n` blocked all step execution — confirmed via trace log (`plan_approval` event, `approved: false`, no subsequent `react_run` steps). `docs/roadmap.md` updated: item moved from "Plausible Future Capabilities" to "Done".
**Notes:** Also found two pre-existing environment issues while doing the manual verification, unrelated to this change, not fixed (out of scope): `python -m agent_harness.cli` produces zero output for any invocation (including `--help`) because `cli.py` has no `if __name__ == "__main__": main()` guard; the installed `agent-harness` console script fails with `ModuleNotFoundError: No module named 'agent_harness'` because its shebang (`.venv/bin/python3.13`) is a symlink to the Homebrew system Python rather than resolving inside the venv. Worked around both by calling `main()` directly via `python -c` for the manual verification run. Flagging for a future session — not blocking, since normal test/CI invocation doesn't go through either path.

---

## Last Session (2026-08-10, model registry consolidation)
**Status:** Ready for Next Phase
**Working on:** Consolidated scattered per-model metadata into one registry (`agent_harness/models.py::MODEL_REGISTRY`), replacing three separately-maintained tables — `budget.py::COST_TABLE`, `context.py::_CONTEXT_LIMITS`, `openai_provider.py::_SUPPORTED_RESPONSE_MODELS`/`_OLDER_GPT5_DEFAULT_MINIMAL_REASONING` — plus hand-duplicated model lists in tests. Motivated by a real, demonstrated bug: `COST_TABLE` had a priced entry for `("openai", "o4-mini")` while `openai_provider.py` hard-rejected all o1/o3/o4 models. Traced via git history: o4-mini support was genuinely built and validated in April (`89dc618`, real H1p3 experiment runs), then silently dropped when the Responses API split landed (`61d32d3`) — nobody removed the now-dead `COST_TABLE` entry. One scattered source of truth couldn't prevent that drift; a single `ModelSpec` per `(provider, model)` — cost and context together — makes a cost-only or context-only entry structurally impossible going forward.

**o-series (o1/o3/o4) decision, made explicitly with the user**: stays completely dropped, no registry entries, no quirk-handling code (the April `max_completion_tokens`/reasoning-param handling was already gone from the codebase, nothing dormant to preserve) — but the explicit `_EXCLUDED_MODEL_PREFIXES` rejection stays, so an o-series model config fails with a clear error rather than a confusing raw API failure three layers down.

**A second simplification fell out of removing the allowlist concept**: `_SUPPORTED_RESPONSE_MODELS` was never real per-model routing data — the only genuine routing signal is whether `base_url` is set (hosted OpenAI vs. an OpenAI-compatible backend like LM Studio). The allowlist was purely a redundant "have we decided to support this" gate — redundant because the agent's own `config.yaml` already decides which model gets used. Dropping it simplified `_response_endpoint_for_model` from three cases to two, and fixed all 10 of `test_provider_openai.py`'s failing tests as a side effect (they used `gpt-4o`/`gpt-4o-mini` as example models, which the user's earlier in-progress trim of the allowlist had excluded).

One real signal preserved rather than guessed: the user's own edit to `test_context.py` (asserting `gpt-5.6-luna`'s context limit as `400_000`, changed from a prior `gpt-5.4-mini` assertion) was honored as intent rather than papered over with a placeholder default — `gpt-5.6-luna` got a real `400_000` entry, not the "unconfirmed" 128k fallback used for its two siblings (`gpt-5.6-terra`/`gpt-5.6-sol`, no equivalent signal for those).

`agents/agent_budgets/instructions.md` updated to point its cross-reference step at the one file (`models.py::MODEL_REGISTRY`) instead of three, with an explicit note that o-series absence is deliberate, not a gap to flag.

Verified: `pytest tests/unit -q` → 492 passed (up from 458, all `test_provider_openai.py` failures now fixed); ruff clean; `mypy --strict` clean across 34 files; radon shows no D-or-worse. Manual: confirmed `o4-mini`/`o1-pro` still raise a clear `ValueError`; real end-to-end run of `gpt-4o-mini` (previously blocked) succeeded with correct cost tracking (`$0.0003/$0.10`).

---

## Prior work this session: two fixes, both from real user reports while using `agent_budgets`.

1. **CLI REPL empty-input crash** — `cli.py:182-188` sent bare-Enter input straight to the Anthropic API as `Message(role="user", content="")`, which the API rejects with an unhandled `400` traceback. Fixed: empty/whitespace-only input now re-prompts, same shape as the existing `exit`/`quit` special-case. TDD: failing test first (`test_repl_mode_skips_empty_input`), then fixed.

2. **Rich markup corruption bug** — `Console.input()`/`console.print()` parse `[...]` as style tags by default. The permission prompt's own hint text (`[o]nce / allow for [s]ession / ...`) was self-corrupting (user-reported screenshot showed the garbled output). Investigation found the same vulnerability class in `display.py` (`show_tool_call`, `show_tool_result`, `show_delta`, `show_thinking_delta`, `show_budget`) — higher-likelihood to actually fire there since `web_fetch`'s markdown output routinely contains `[link](url)` syntax and `edit_file`/`content_search` args routinely contain bracket-bearing code. User confirmed fixing the full scope, not just the reported prompt. Fix: wrap all dynamic/bracket-bearing content in `rich.text.Text(...)` (structurally immune to markup parsing regardless of the `markup` flag, and composable so static styling + dynamic content can still share one line); `markup=False` on the two now-fully-static input hint strings. TDD throughout, covered with real-content assertions (not just "no crash") via a `Console(record=True)` recorder pattern in `test_display.py`.

3. **`list_provider_models` tool + `agent_budgets` scope fix** — user's design critique: scoping `agent_budgets` to the harness's own "supported models" lists (`COST_TABLE`, `_CONTEXT_LIMITS`, `_SUPPORTED_RESPONSE_MODELS`) repeats the exact bug the provider-registry fix solved one layer up — those lists are exactly what might be incomplete, so an agent that scopes from them can never discover a model missing from all three. Confirmed both Anthropic and OpenAI SDKs expose real `models.list()` endpoints. Built `list_provider_models(provider: str) -> str` in `tools.py` (validates against the providers registry, calls the vendor SDK directly, TDD'd, wired into `network_exfiltration_blocker`). Explicitly confirmed with the user that this does **not** touch `_SUPPORTED_RESPONSE_MODELS` — that stays a hard runtime compatibility gate (`openai_provider.py`'s Responses-API routing), a different concern from "does this model currently exist at the vendor." `agent_budgets`'s `instructions.md`/`config.yaml` updated to call `list_provider_models` as the real scope source, with `content_search` now used only for the internal cross-table consistency check, not for scoping. Verified with a real run: correctly filtered noisy vendor output (embeddings/audio/image excluded) to mainline chat models, and surfaced genuine gaps invisible to the old approach (`gpt-4.1`, `gpt-5`, several current Claude models — none of which appear anywhere in this codebase, so `content_search` alone could never have found them). $0.0137 of $0.30 budget.

Tests: 472 unit passed, ruff/mypy --strict clean, 3 new real (zero-mock) integration tests against live Anthropic/OpenAI APIs (`TestListProviderModelsReal`), no mocks in `tests/integration/` (verified via grep gate check).

4. **`agent_budgets` REPL crash — dangling `tool_use` on budget exhaustion.** Real user report: agent hit its turn/cost limit mid tool-call ("Turn 15/15"), then a follow-up REPL prompt crashed with `anthropic.BadRequestError: tool_use ids were found without tool_result blocks`. Root cause in `loops/react.py::run`: the `on_budget` break happened right after `messages.append(response.message)`, *before* the turn's pending tool calls were executed — so a response with `stop_reason == "tool_use"` that also tripped the budget check left its `tool_use` block permanently unresolved in the message list, which is invalid for the next API call. Fix: defer the budget-exceeded break until *after* the turn's tool calls are resolved into `tool_result` messages (the budget check itself, `budget.record()`, still runs once per turn as before — only the `break` timing moved). Root cause found by updating an existing test (`test_on_budget_stops_loop` in `test_react_loop.py`) that only asserted `chat_fn.call_count`, never checked whether the pending tool call got resolved — added `test_on_budget_still_resolves_pending_tool_call` to lock in the correct behavior.

5. **Silent budget-exhaustion stop.** Related report: when the agent stops because it hit `max_turns`/`max_cost`, there was no clear signal distinguishing that from a normal per-turn budget status line — user only saw the same `"Turn N/M | $X.XX"` text every turn, exceeded or not. Fixed in `runtime.py::_make_callbacks`'s `on_budget`: when `budget.record()` returns exceeded, the shown line now reads `"...— stopping (budget limit reached, task may be incomplete)"`.

Both verified end-to-end with a real run (not just unit tests): `agents/hello --max-turns 1` forced into a multi-tool-call turn that exceeds budget mid-turn, followed by a second REPL prompt — confirmed the stopping notice appears, the second prompt runs cleanly with no crash, exit code 0.

Tests (this batch): 475 unit passed, ruff/mypy --strict clean, radon shows `react.py::run` at C (was previously lower; acceptable — project's own bar is "no D or worse").

6. **React loop turn/budget awareness** (roadmap item, added 2026-04-09, now resolved) — planned properly via plan mode rather than jumping straight to code, since it touched loop/callback/session-persistence architecture. Codebase survey confirmed react.py is the only loop with a genuine per-turn budget concept (others reuse `max_turns` for a different unit, or delegate to a nested `react_run`). The literal roadmap wording ("inject into the system prompt") would have been a real bug: `init_messages()` only rebuilds the system prompt when the message list is empty, so mutating the canonical `messages[0]` in place would bake a stale "Turn 8/10" note into any resumed session forever. Fix: `Budget.status_note()` (new method, TDD'd) computes a "You have N turn(s) remaining[, Estimated $X remaining]" line; wired through a new `LoopCallbacks.get_budget_status` field into `runtime.py::_make_callbacks`; `loops/react.py::_with_budget_note` builds a **disposable per-call overlay** (`call_messages`) each turn and passes that to `chat_fn`, while the canonical, session-persisted `messages` list is never touched. Confirmed both Anthropic and OpenAI provider converters extract the system message the same way regardless of source, so this works uniformly with no per-provider special-casing. Verified live: the note correctly decrements turn-by-turn in a real run (`--verbose` debug logs), and a saved session's system message was directly inspected and confirmed to carry no injected note. `docs/roadmap.md` updated, item moved to Done.

**Unrelated finding, not touched**: `agent_harness/providers/openai_provider.py` has separate uncommitted changes on disk (not made by me) that narrow `_SUPPORTED_RESPONSE_MODELS` and cause 10 pre-existing test failures in `test_provider_openai.py`. Flagged to the user and confirmed intentional/in-progress — left untouched, out of scope for this work.

## Last Session (2026-08-06)
**Status:** Ready for Next Phase
**Working on:** Four new tools (`web_fetch`, `edit_file`, `web_search` via Tavily, `content_search`) plus a new `agent_budgets` example agent that keeps `budget.py::COST_TABLE` honest against real published pricing. Plan at `/Users/simonfrank/.claude/plans/abstract-doodling-wren.md`. Scope corrected mid-plan: provider/model scope for `agent_budgets` comes from `agent_harness/providers/__init__.py`'s registry, not `COST_TABLE` itself (the table being checked can't also be the source of truth for what to check) — this exact design decision paid off in the real verification run below.
**Next step:** Done and verified end-to-end with a real run. Nothing left mid-build. `docs/features.md`/`docs/roadmap.md` updated to match.
**Notes:** Swapped `beautifulsoup4` for `trafilatura` mid-build (content-extraction, not just HTML->markdown reformatting — discards nav/ads/boilerplate). Verified actively maintained (release 2026-07-31) despite a 2019 first release, before committing to it. Real dependency cost: pulls in 8 packages including `lxml` (compiled C-extension) — flagged transparently, proceeded anyway since the maintenance/functionality case held up. `pyproject.toml` now declares `httpx`+`trafilatura` directly. `web_fetch`/`web_search` both wired into `network_exfiltration_blocker`'s `_has_network_intent` (previously only inspected `run_command`/`execute_code`) — verified via failing-test-first that skipping this would have silently bypassed the domain-approval safety net. `TAVILY_API_KEY` already present in `.env`, confirmed via a real (not skipped) integration test against the live Tavily API.

**Real end-to-end verification run (2026-08-06)** — ran `agent_budgets` for real against live Anthropic/OpenAI/Tavily. Confirmed working exactly as designed: scope came from the providers registry (agent explicitly checked both `anthropic` and `openai`, not just what happened to already be in `COST_TABLE`); the cross-table `content_search` check caught the *exact* known `o4-mini`/`_CONTEXT_LIMITS` gap flagged earlier this session; real pricing pages were fetched and compared, surfacing genuine drift (Anthropic's current model lineup — Sonnet 5/Opus 5 — vs. the codebase's stale `claude-*-4-6`/`4-5-20251001` identifiers); two `edit_file` calls correctly paused for human approval (`always_ask` in config), both denied in this run, and `git diff` on `budget.py`/`context.py` confirmed **zero actual file changes** — the deny genuinely blocked the write, not just logged it. The agent also correctly *declined* to propose a fix for the ambiguous Opus case, explaining that it involved a model-version policy decision beyond its scope — matches the "don't guess" instruction directly. Ran within budget: 10/15 turns, $0.13 of $0.30. No pricing fixes were actually applied to the repo — that's a deliberate human decision for a future session, not an oversight.

Tests: 453 unit passed, ruff/mypy --strict clean, no new radon D-or-worse, 3 new real (zero-mock) integration tests (`edit_file`, `web_fetch` via local HTTP server, `web_search` via live Tavily API) all passing.

## Last Session (2026-08-03)
**Status:** In Progress
**Working on:** New `eval/` package — general-purpose evaluation framework (test cases, code + model graders with gate/signal split, JSONL storage, ranked leaderboard report), replacing the ad hoc `scripts/run_experiment.py` pattern. Plan approved and saved at `/Users/simonfrank/.claude/plans/abstract-doodling-wren.md`. Streaming + extended thinking work (Anthropic + OpenAI-modern) from earlier this session is done — see the plan file history / `docs/streaming-plan.md` for that.
**Next step:** Build order per the plan: `eval/types.py` -> `Budget`/`PreparedRuntime` additive changes -> `eval/storage.py` -> `eval/cases.py` -> `eval/graders/code/*` + discovery -> `eval/graders/model.py` -> `eval/runner.py` -> `eval/report.py` -> `eval/cli.py` -> `docs/eval_framework.md` + `docs/roadmap.md` update -> capstone column-matcher suite + real run.
**Notes:** `column_match` grader reuses `scripts/score_run.py::score()` almost verbatim (per user's explicit ask for "a matcher evaluator in the new approach") rather than reimplementing comparison logic. `eval/` is a sibling top-level package, not inside `agent_harness/` — deliberate, matches how `scripts/` already consumes the framework rather than extending it.
**Notes:** Resolves `docs/streaming-plan.md`'s outstanding decisions #1 and #3. `on_delta`/`on_thinking_delta` callbacks take `(agent_id, chunk)` to match the roadmap's L3 sink design without a future breaking change; parallel-agent/RunContext work itself is explicitly out of scope. Extended thinking stays Anthropic-only (OpenAI has no readable reasoning-content extraction, only the pre-existing `reasoning_effort` knob). Streaming is now valid for `anthropic` and `openai` (Responses API models only — rejected for OpenAI's Chat Completions/`base_url` path, both at the provider boundary and in `validate_config`).
Verified: `.venv/bin/pytest tests/unit -q` (excluding two pre-existing unrelated failures in test_value_overlap.py/test_profile_data.py from a missing openpyxl/expat dependency) -> `326 passed`. `.venv/bin/ruff check agent_harness tests` -> `All checks passed!`. `env MYPYPATH=. .venv/bin/mypy --strict --explicit-package-bases agent_harness tests` -> `Success: no issues found in 71 source files`. `radon cc --min D agent_harness` -> only the pre-existing accepted D (`_to_anthropic_messages`), no new D/worse.
Integration: Anthropic streaming + thinking real-API tests pass (`tests/integration/test_end_to_end.py::TestStreamingIntegration`, 2/2). Full `tests/integration -q` run: `56 passed, 6 failed`, all 6 pre-existing OpenAI failures from `insufficient_quota` (account has no credits), unrelated to code. The new OpenAI streaming integration test (`TestOpenAIStreamingIntegration`) was written and run but also hit `insufficient_quota` — the error was a retryable `api_error` (429), not `BadRequestError`, meaning the request reached OpenAI correctly shaped; it just couldn't complete. **Not fully integration-verified end-to-end** — revisit once the OpenAI account has credits.

---

# Progress Tracker

## Phase 1 — A Working Agent ✅

| Component | Unit Tests | Code | Integration Tests |
|-----------|-----------|------|-------------------|
| types.py | ✅ (10) | ✅ | ⏭️ N/A |
| tools.py + discovery | ✅ (24) | ✅ | ✅ |
| budget.py | ✅ (6) | ✅ | ✅ |
| display.py | ✅ (7) | ✅ | ⏭️ N/A |
| providers/anthropic.py | ✅ (11) | ✅ | ✅ (4) |
| providers/openai_provider.py | ✅ (25) | ✅ | ✅ (6) |
| providers/retry.py | ✅ (via provider tests) | ✅ | ✅ |
| config.py | ✅ (9) | ✅ | ✅ |
| cli.py + overrides | ✅ (20) | ✅ | ⏭️ N/A |
| 7 example agents | ⏭️ N/A | ✅ | ✅ (smoke tested) |

## Phase 2 — Security and Reliability ✅

| Component | Unit Tests | Code | Integration Tests |
|-----------|-----------|------|-------------------|
| hooks.py | ✅ (48) | ✅ | ✅ (5) |
| network.py | ✅ (via hooks) | ✅ | ✅ |
| permissions.py | ✅ (9) | ✅ | ✅ (3) |
| log.py | ✅ (4) | ✅ | ✅ (1) |
| trace.py | ✅ (4) | ✅ | ✅ (1) |

## Phase 3 — Multi-Provider and Loop Patterns ✅

| Component | Unit Tests | Code | Integration Tests |
|-----------|-----------|------|-------------------|
| loops/react.py | ✅ (6) | ✅ | ✅ (2) |
| loops/plan_execute.py | ✅ (7) | ✅ | ✅ (1) |
| loops/rewoo.py | ✅ (5) | ✅ | ✅ (1) |
| loops/reflection.py | ✅ (5) | ✅ | ✅ (2) |
| loops/eval_optimize.py | ✅ (6) | ✅ | ✅ (2) |
| loops/ralph.py | ✅ (5) | ✅ | ✅ (1) |
| loops/debate.py | ✅ (4) | ✅ | ✅ (1) |
| loops/common.py | ⏭️ N/A | ✅ | ✅ (via loops) |
| context.py | ✅ (8) | ✅ | ⏭️ N/A |

## Phase 4 — Memory, Routing, and Agent Building ✅

| Component | Unit Tests | Code | Integration Tests |
|-----------|-----------|------|-------------------|
| session.py | ✅ (4) | ✅ | ✅ (1) |
| memory.py | ✅ (7) | ✅ | ✅ (1) |
| routing.py | ✅ (7) | ✅ | ✅ (1) |
| scaffold.py | ✅ (6) | ✅ | ⏭️ N/A |
| skills.py | ✅ (6) | ✅ | ✅ (2) |

## Phase 5 — Polish and Examples ✅

| Component | Status |
|-----------|--------|
| 7 example agents | ✅ All run successfully |
| 2 custom tools (tools/) | ✅ |
| 2 shared skills (skills/) | ✅ |
| CLI entry point | ✅ |
| CLI config overrides | ✅ |
| README | ✅ |
| MIT License | ✅ |
| example_runs.md | ✅ |

## Phase 6 — OWASP Hardening and Observability ✅

| Component | Unit Tests | Code | Integration Tests |
|-----------|-----------|------|-------------------|
| Memory poisoning defence | ✅ (2) | ✅ | ✅ (1) |
| Cascading depth limit | ✅ (2) | ✅ | ✅ (1) |
| Structured traces (JSONL) | ✅ (4) | ✅ | ✅ (1) |
| Context-loaded event | ⏭️ N/A | ✅ | ✅ (via traces) |

### Quality Gates
- ruff: ✅ All checks passed
- mypy --strict: ✅ No issues (32 files)
- radon cc --min C: ✅ 1 D-rated function (acceptable — Anthropic message translation)
- Tests: ✅ 296 total (240 unit + 56 integration)
- Integration mock check: ✅ Zero mocks in tests/integration/

## Phase 7 — Streaming and Extended Thinking ✅

| Component | Unit Tests | Code | Integration Tests |
|-----------|-----------|------|-------------------|
| Anthropic streaming + thinking | ✅ (25) | ✅ | ✅ (2) |
| OpenAI-modern streaming (Responses API) | ✅ (28) | ✅ | 🟡 written, blocked on account credits |
| LoopCallbacks on_delta/on_thinking_delta | ✅ | ✅ | ✅ (via providers) |
| stream/show_thinking config + CLI flags | ✅ | ✅ | ⏭️ N/A |
| Session persistence of thinking blocks | ✅ | ✅ | ⏭️ N/A |

### Quality Gates
- ruff: ✅ All checks passed
- mypy --strict: ✅ No issues (71 files)
- radon cc --min D: ✅ same 1 accepted D as before, no regression
- Tests: 326 unit passed (2 pre-existing unrelated failures excluded), 56 integration passed / 6 failed (OpenAI `insufficient_quota`, account issue not code)

## Phase 8 — Evaluation Framework (`eval/`) 🟡 In Progress

| Component | Unit Tests | Code | Integration Tests |
|-----------|-----------|------|-------------------|
| eval/types.py | ✅ (7) | ✅ | ⏭️ N/A |
| Budget/PreparedRuntime additions | ✅ (3) | ✅ | ⏭️ N/A |
| eval/storage.py | ✅ (5) | ✅ | ⏭️ N/A |
| eval/cases.py | ✅ (5) | ✅ | ⏭️ N/A |
| eval/graders/code (+ discovery) | ✅ (11) | ✅ | ⏭️ N/A |
| eval/graders/model.py | ✅ (4) | ✅ | ⏭️ N/A |
| eval/runner.py | ✅ (12) | ✅ | ❌ |
| eval/report.py | ✅ (13) | ✅ | ⏭️ N/A |
| eval/cli.py | ✅ (9) | ✅ | ⏭️ N/A |
| Column-matcher capstone suite | ⏭️ N/A | ✅ | ✅ (1, real API, H0 + H4 cells) |

### Environment fix (2026-08-03)

The capstone's first real run failed — not a code bug, but a pre-existing
`expat` C-extension mismatch on the Python 3.14.5 Homebrew bottle (linked
against macOS's system `libexpat`, missing a symbol `pyexpat` expected —
macOS 26.2's bundled libexpat hasn't caught up). This broke `openpyxl`/
`pandas.read_excel` entirely, which is why `test_value_overlap.py` and part
of `test_profile_data.py` were already failing/excluded all session before
this was even diagnosed. **Fix:** recreated `.venv` against `python@3.13`
(already installed via Homebrew, confirmed working `pyexpat`) — old venv
preserved at `.venv-py314-backup` pending cleanup. Reinstalled via
`pip install -e ".[dev]"` plus `pandas`/`openpyxl`/`pandas-stubs`. This
picked up newer unpinned versions of several deps (anthropic 0.89→0.120,
openai 2.30→2.52, mypy 1.20→2.3, pytest/ruff/rich bumped) — verified none of
this broke anything already built. One follow-on issue: numpy's latest
release (2.5.1) ships stubs using Python 3.12+ `type` statement syntax,
which fails mypy's `python_version = "3.11"` target (`pyproject.toml`) —
pinned `numpy==2.4.4` (previously-working version) to resolve; unrelated to
the Python 3.13 switch itself.

**Verified on the fixed environment:**
- `.venv/bin/pytest tests/unit -q` → `428 passed` (no exclusions needed —
  `test_value_overlap.py`/`test_profile_data.py` pass now too).
- `.venv/bin/ruff check agent_harness eval tests` → `All checks passed!`
- `env MYPYPATH=. .venv/bin/mypy --strict --explicit-package-bases agent_harness eval tests` → `Success: no issues found in 92 source files`
- `.venv/bin/pytest tests/integration -q` → `57 passed, 7 failed` — all 7
  failures are OpenAI `insufficient_quota` (account credits), unrelated.
  Every Anthropic-backed test passed, including the new capstone.

### Quality Gates (core eval/ package)
- ruff: ✅ All checks passed
- mypy --strict: ✅ No issues (92 files)
- radon cc --min D: ✅ same 1 pre-existing accepted D, no new D/worse (`discover_code_graders`/`summarize` both C, matching `discover_tools`'s own C rating)
- Tests: ✅ 428 unit passed, 57 integration passed (7 OpenAI-credit failures excluded, unrelated)
